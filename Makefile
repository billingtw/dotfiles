# or use `pwd`. ex: ln -fs `pwd`/vim ~/.vim
init:
ln -fs ~/dotfiles/vimrc ~/.vimrc
ln -fs ~/dotfiles/vim ~/.vim
ln -fs ~/dotfiles/bashrc ~/.bashrc
ln -fs ~/dotfiles/bash_profile ~/.bash_profile
ln -fs ~/dotfiles/cshrc ~/.cshrc

